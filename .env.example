DATABASE_URL="postgresql://patient_app:patient_app_password@localhost:5432/patient_bmi?schema=public"

# Generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY="replace-with-a-32-byte-base64-key"
SEARCH_INDEX_KEY="replace-with-a-different-32-byte-base64-key"
